# Frontend example environment variables
# Copy to frontend/.env and fill values before running

# Backend base URL (no trailing /api). Example: http://localhost:5000
VITE_BACKEND_URL=http://localhost:5000

# Gemini API key (optional)
VITE_GEMINI_API_KEY=
